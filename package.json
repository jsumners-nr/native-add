{
  "name": "@jsumners-nr/native-add",
  "version": "1.0.0",
  "main": "./binding.js",
  "scripts": {
    "install": "node-gyp-build",
    "build": "node build.js",
    "test": "node --test"
  },
  "dependencies": {
    "nan": "^2.18.0",
    "node-gyp-build": "^4.8.0"
  },
  "devDependencies": {
    "prebuildify": "^6.0.0"
  }
}
